'use strict';

module.exports = (router, models) => {
  let Neighborhood = models.Neighborhood;

  router.route('/neighborhoods')
    .get((req, res) => {

    })
    .post((req, res) => {

    });
};
